"""this will load all the curated courses, sections, meeting detail, 
check supported preruisities and degree requirements and categories"""
from fastapi import APIRouter

# Initialize the router for this specific module
router = APIRouter()